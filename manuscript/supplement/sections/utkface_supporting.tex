\section{Historical supporting evidence: UTKFace, seed 0}

UTKFace is historical supporting evidence only. The upper endpoint is age-bin
class 4; the frozen sorted-filename stratified split has a test endpoint support
of 67. Only CE/RPS seed-0 backbone artifacts are available. Seeds 1--4 are
absent for both objectives, so a prospective supporting replication would
require eight new backbones. It is consequently excluded from the main
confirmatory H1/H2a block.

\begin{center}
\begin{tabular}{lrrrrrr}
\toprule
Objective & Condition & C4 exact & C4 MAE & C4 shrinkage & C4 $p_4$ & C4 severe \\
\midrule
CE & A (historical) & 35/67 & .612 & .755 & .499 & .104 \\
CE & C (historical) & 39/67 & .507 & .582 & .577 & .060 \\
\bottomrule
\end{tabular}
\captionof{table}{Historical UTKFace seed-0 upper-endpoint results from the Phase 3.13 frozen-head mechanism run, not the separate Figure~1 original-CE aggregate.}
\end{center}

The retained supporting mechanism run reports a CE direction-only C condition
with fixed original norms and biases; this table is descriptive and is not a
new H1 test. Frozen source artifacts retain RPS seed-0 provenance as well, but
the current supplement does not add unverified A/C RPS aggregates.
